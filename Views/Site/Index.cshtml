@model IEnumerable<WebSiteAdminPanel.Models.Site>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<section id="main">
    <h3>Site Bilgileri</h3>
    @Html.Action("GridViewPartial", "Site")
    @foreach (var item in Model)
    {
        @Html.ActionLink("Güncelle", "Edit", new { id = item.SiteId })
    }
    @*<table class="table table-bordered table-striped table-responsive-sm">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Baslik)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.AnahtarKelimeler)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Tanim)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.LogoUrl)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Unvan)
                </th>
                <th>İşlemler</th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Baslik)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.AnahtarKelimeler)
                    </td>
                    <td>
                        @Html.Raw(item.Tanim)
                    </td>
                    <td>
                        <img src="@item.LogoUrl" width="100" height="100" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Unvan)
                    </td>
                    <td>
                        @Html.ActionLink("Güncelle", "Edit", new { id = item.SiteId })
                    </td>
                </tr>
            }
        </table>*@
</section>





